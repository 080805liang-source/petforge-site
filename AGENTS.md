# Project Instructions

1. Think through requirements and assumptions before editing.
2. Keep changes simple and avoid unnecessary abstractions.
3. Only edit files directly related to the request.
4. Put project files, dependencies, generated assets, data, caches, and temporary outputs on `D:` whenever controllable. Do not use `C:` as the project storage location.
